import XCTest
@testable import VeliaData

final class RepositoriesTests: XCTestCase {
    func testProtocolExists() {
        XCTAssertTrue(true)
    } // replaced by in-memory repo tests in Phase 1
}
